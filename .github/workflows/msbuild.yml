name: MSBuild
on: [push]

env:
  BUILD_CONFIGURATION: Release

permissions:
  contents: read

jobs:
  build:
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v3

    # - name: Add MSBuild to PATH
      # uses: microsoft/setup-msbuild@v1.0.2

    # - name: Build 64bit
      # working-directory: ${{env.GITHUB_WORKSPACE}}
      # run: msbuild /m "/p:Configuration=${{env.BUILD_CONFIGURATION}};Platform=x64;OutDir=output/64bit/" .

    # - name: Build 32bit
      # working-directory: ${{env.GITHUB_WORKSPACE}}
      # run: msbuild /m "/p:Configuration=${{env.BUILD_CONFIGURATION}};Platform=x86;OutDir=output/32bit/" .

    # - name: Make zip 
      # run: Compress-Archive "output/*" "output/HelloWorld.windows.zip"
      
    # - name: List files in the repository
      # run: ls -r ${{ github.workspace }}
    
    # - name: Upload zip
      # uses: actions/upload-artifact@v3
      # with:
        # name: HelloWorld.windows.zip
        # path: output/HelloWorld.windows.zip
        
    - name: Windows build
      uses: ./.github/actions/windowsbuild
      with:
        solution-filename: HelloWorld.sln
        zip-filename: HelloWorld_windows